# RCG Predictions for CMB-S4 and LiteBIRD

Quantitative, falsifiable targets derived from the RCG anomaly sector, Q-boundary mode, screening geometry, and chiral extension.

## 1. Low-ℓ Temperature (ℓ ≤ 30)

**Observable**: Quadrupole–octopole alignment (Q–O alignment)

**RCG Prediction**:
- Alignment axis = û (global anisotropy direction)
- Angular separation between ℓ=2 and ℓ=3 principal axes: **Δθ_QO < 10°**

**Experimental Targets**:
- LiteBIRD: σ(Δθ_QO) ≈ 3°
- CMB-S4: σ(Δθ_QO) ≈ 2°

**Pass/Fail**:
- **FAIL** if Δθ_QO > 15°
- **PASS** if Δθ_QO < 10° and axis matches û within 10°

## 2. Large-Angle Correlation Suppression

**Observable**: C(θ) for θ > 60°

**RCG Prediction**:
- Screening length d_s produces hard cutoff: **C(θ > 60°) ≈ 0**
- Quantitatively: **|C(θ > 60°)| < 5 × 10⁻⁵**

**Experimental Targets**:
- LiteBIRD: σ[C(θ > 60°)] ≈ 2 × 10⁻⁵

**Pass/Fail**:
- **FAIL** if |C(θ > 60°)| > 1 × 10⁻⁴
- **PASS** if |C(θ > 60°)| < 5 × 10⁻⁵

## 3. Anisotropic Lensing (α₁, α₂)

**Definitions**: α₁ = dipole modulation, α₂ = quadrupole modulation

**RCG Prediction**:
- α₁ = k_D A_D, α₂ = k_Q A_Q
- **|α₁|, |α₂| < 0.01** (expected range 0.002–0.008)

**Experimental Sensitivity**:
- CMB-S4: σ(α₁), σ(α₂) ≈ 0.002

**Pass/Fail**:
- **FAIL** if |α₁|, |α₂| < 0.001 (too small) or > 0.01 (too large)
- **PASS** if 0.002–0.008 and aligned with û

## 4. Parity-Odd Spectra (TB, EB)

**RCG + Chiral Gravity Prediction**:
- TB and EB localized at **ℓ = 2–3**, rapid fall-off for ℓ > 30
- Amplitude: **C₂^{TB}, C₃^{TB} ~ 10⁻³ – 10⁻² μK²** (same for EB)
- Alignment with Q-boundary axis û

**Experimental Sensitivity**:
- LiteBIRD: σ(C₂^{TB}), σ(C₂^{EB}) ≈ 3 × 10⁻³ μK²

**Pass/Fail**:
- **FAIL** if isotropic, misaligned, or detected at ℓ > 30
- **PASS** if detected at ℓ = 2–3 with correct axis

## 5. Chiral ISW Amplitude (γ_Π)

**RCG Prediction**:
- **|γ_Π| ≈ (1–5) × 10⁻³**

**Experimental Sensitivity**:
- LiteBIRD: σ(γ_Π) ≈ 1 × 10⁻³

**Pass/Fail**:
- **FAIL** if |γ_Π| < 5 × 10⁻⁴ or > 10⁻²
- **PASS** if in (1–5) × 10⁻³ range

## 6. Cold Spot Signature

**RCG Prediction**:
- ρ₋ defect → tension scar
- **|κ_D| < 0.005**, **|ΔEE| < 0.5 μK²**, **|EB| < 0.5 μK²**

**Experimental Sensitivity**:
- CMB-S4: σ(κ_D) ≈ 0.002

**Pass/Fail**:
- **FAIL** if κ_D > 0.01 (strong mass signature)
- **PASS** if κ_D < 0.005 and no mass overdensity

## 7. High-ℓ Isotropy

**RCG Prediction**:
- High-ℓ TT/TE/EE remain ΛCDM-like
- **|ΔC_ℓ / C_ℓ| < 0.5%** for ℓ > 100

**Experimental Sensitivity**:
- CMB-S4: σ(ΔC_ℓ / C_ℓ) ≈ 0.2%

**Pass/Fail**:
- **FAIL** if deviations > 1%
- **PASS** if deviations < 0.5%

## 8. Master Signature: Axis Alignment

**Prediction**: Single preferred axis û across **all** observables (T, φ, TB/EB, ISW) with **Δθ < 10°**

**Pass/Fail**:
- **FAIL** if axes differ by > 15°
- **PASS** if all agree within 10°

## 9. Summary of Falsifiable Targets

**RCG is falsified if ANY of these occur**:
- No Q–O alignment (Δθ_QO > 15°)
- No large-angle suppression
- |α₁|, |α₂| outside 0.002–0.01
- TB/EB not localized at ℓ = 2–3 or misaligned
- |γ_Π| outside expected range
- Cold Spot shows strong mass signature
- High-ℓ isotropy violated > 1%

**RCG is supported if ALL hold**:
- Axis-aligned low-ℓ anomalies
- Finite-range correlation cutoff
- Correct α₁/α₂ amplitudes
- Low-ℓ TB/EB with correct axis
- Chiral ISW in predicted range
- Cold Spot = tension defect
- High-ℓ spectra remain ΛCDM-like
