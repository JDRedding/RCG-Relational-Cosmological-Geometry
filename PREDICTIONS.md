# RCG PREDICTIONS FOR CMB-S4 AND LITEBIRD
Quantitative, falsifiable targets derived from the RCG anomaly sector,
Q-boundary mode, screening geometry, and chiral extension.

1. LOW-ℓ TEMPERATURE (ℓ ≤ 30)

Observable:
    Quadrupole–octopole alignment (Q–O alignment)

RCG Prediction:
    • Alignment axis = û (global anisotropy direction)
    • Angular separation between ℓ=2 and ℓ=3 principal axes:
          Δθ_QO  < 10°

Experimental Targets:
    • LiteBIRD: σ(Δθ_QO) ≈ 3°
    • CMB-S4:   σ(Δθ_QO) ≈ 2°

Pass/Fail:
    • FAIL if Δθ_QO > 15°
    • PASS if Δθ_QO < 10° and axis matches û within 10°

2. LARGE-ANGLE CORRELATION SUPPRESSION

Observable:
    C(θ) for θ > 60°

RCG Prediction:
    • Screening length d_s produces:
          C(θ > 60°) ≈ 0  (hard cutoff)
    • Quantitatively:
          |C(θ > 60°)| < 5 × 10⁻⁵

Experimental Targets:
    • LiteBIRD: σ[C(θ > 60°)] ≈ 2 × 10⁻⁵

Pass/Fail:
    • FAIL if |C(θ > 60°)| > 1 × 10⁻⁴
    • PASS if |C(θ > 60°)| < 5 × 10⁻⁵

3. ANISOTROPIC LENSING (α₁, α₂)

Definitions:
    α₁ = dipole lensing modulation
    α₂ = quadrupole lensing modulation

RCG Prediction:
    α₁ = k_D A_D
    α₂ = k_Q A_Q
    with |α₁|, |α₂| < 0.01

Quantitative Targets:
    • α₁, α₂ expected in range:
          0.002 < |α₁|, |α₂| < 0.008

Experimental Sensitivity:
    • CMB-S4 lensing reconstruction:
          σ(α₁), σ(α₂) ≈ 0.002

Pass/Fail:
    • FAIL if |α₁|, |α₂| < 0.001 (too small)
    • FAIL if |α₁|, |α₂| > 0.01  (too large)
    • PASS if 0.002–0.008 and aligned with û

4. PARITY-ODD SPECTRA (TB, EB)

RCG + Chiral Gravity Prediction:
    • TB and EB localized at ℓ = 2–3
    • Rapid fall-off for ℓ > 30
    • Amplitude:
          C₂^{TB}, C₃^{TB}  ~ 10⁻³ – 10⁻² μK²
          C₂^{EB}, C₃^{EB}  ~ 10⁻³ – 10⁻² μK²
    • Alignment with Q-boundary axis û

Experimental Sensitivity:
    • LiteBIRD:
          σ(C₂^{TB}) ≈ 3 × 10⁻³ μK²
          σ(C₂^{EB}) ≈ 3 × 10⁻³ μK²

Pass/Fail:
    • FAIL if TB/EB isotropic or misaligned with û
    • FAIL if TB/EB detected at ℓ > 30
    • PASS if TB/EB detected at ℓ = 2–3 with correct axis


5. CHIRAL ISW AMPLITUDE (γ_Π)

RCG Prediction:
    γ_Π = 2 ∫ (∂β_Π/∂η) F_Π dη
    Expected magnitude:
        |γ_Π| ≈ (1–5) × 10⁻³

Experimental Sensitivity:
    • LiteBIRD ISW reconstruction:
          σ(γ_Π) ≈ 1 × 10⁻³

Pass/Fail:
    • FAIL if |γ_Π| < 5 × 10⁻⁴ (too small to match TB/EB)
    • FAIL if |γ_Π| > 10⁻² (too large for Planck bounds)
    • PASS if (1–5) × 10⁻³

6. COLD SPOT SIGNATURE

RCG Prediction:
    • ρ₋ defect → tension scar
    • Weak lensing convergence:
          |κ_D| < 0.005
    • Polarization modulation:
          |ΔEE| < 0.5 μK²
          |EB|   < 0.5 μK²

Experimental Sensitivity:
    • CMB-S4 κ-map:
          σ(κ_D) ≈ 0.002

Pass/Fail:
    • FAIL if κ_D > 0.01 (too massive)
    • PASS if κ_D < 0.005 and no mass overdensity


7. HIGH-ℓ ISOTROPY

RCG Prediction:
    • High-ℓ TT, TE, EE remain ΛCDM-like
    • Constraint:
          |ΔC_ℓ / C_ℓ| < 0.5%  for ℓ > 100

Experimental Sensitivity:
    • CMB-S4:
          σ(ΔC_ℓ / C_ℓ) ≈ 0.2%

Pass/Fail:
    • FAIL if deviations > 1%
    • PASS if deviations < 0.5%


8. MASTER SIGNATURE: AXIS ALIGNMENT

Across all observables (T, φ, TB/EB, ISW), RCG predicts:

    • A single preferred axis û
    • Alignment tolerance:
          Δθ < 10° across all anomaly-sector observables

Pass/Fail:
    • FAIL if axes differ by > 15°
    • PASS if all axes agree within 10°


9. SUMMARY OF FALSIFIABLE TARGETS

RCG is falsified if ANY of the following occur:

    • No Q–O alignment (Δθ_QO > 15°)
    • No large-angle suppression
    • |α₁|, |α₂| outside 0.002–0.01
    • TB/EB not localized at ℓ = 2–3
    • TB/EB misaligned with û
    • |γ_Π| outside (5×10⁻⁴ – 10⁻²)
    • Cold Spot shows strong mass signature
    • High-ℓ isotropy violated > 1%

RCG is supported if ALL of the following occur:

    • Axis-aligned low-ℓ anomalies
    • Finite-range correlation cutoff
    • Correct α₁, α₂ amplitudes
    • Low-ℓ TB/EB detection with correct axis
    • Chiral ISW amplitude in predicted range
    • Cold Spot = tension defect, not mass overdensity
    • High-ℓ spectra remain ΛCDM-like
