PARITY-ODD POWER SPECTRA (TB, EB) IN RCG + CHIRAL GRAVITY

    • definitions of a_{ℓm}^{T,Π}, a_{ℓm}^{E/B,Π}
    • TB, EB formulas
    • LOW-ℓ SCALING BLOCK  << insert here
    • COVARIANCE BLOCK     << insert here
    • predictions

=========================================================

Temperature multipoles:
    a_{ℓm}^T =
        a_{ℓm}^{T,iso}
      + a_{ℓm}^{T,Q}
      + a_{ℓm}^{T,D}
      + a_{ℓm}^{T,Π}

Chiral temperature piece:
    a_{ℓm}^{T,Π} ∝ γ_Π ⟨ Y_{ℓm}, Π M_{2,3} ⟩

Polarization multipoles:
    a_{ℓm}^E, a_{ℓm}^B

Chiral B-mode sourcing:
    a_{ℓm}^{B,Π} ∝ ε_Π ⟨ {}_2Y_{ℓm}, Π M_{2,3} ⟩

    where:
        ε_Π = strength of chiral coupling into B-modes

Parity-odd spectra:

    C_ℓ^{TB} =
        (1 / (2ℓ + 1)) Σ_m
            Re[ a_{ℓm}^{T,Π} (a_{ℓm}^{B,Π})* ]

    C_ℓ^{EB} =
        (1 / (2ℓ + 1)) Σ_m
            Re[ a_{ℓm}^{E,Π} (a_{ℓm}^{B,Π})* ]

Structurally:
    C_ℓ^{TB}, C_ℓ^{EB} ∼
        f_ℓ( A_Q, A_D, γ_Π, ε_Π ; Π M_{2,3}, d^ )

    i.e. they are determined by:
        • low-ℓ anomaly amplitudes A_Q, A_D
        • chiral ISW amplitude γ_Π
        • chiral polarization coupling ε_Π
        • geometry of Π M_{2,3} and preferred direction d^

LOW-ℓ SCALING OF PARITY-ODD SPECTRA (TB, EB) IN RCG + CHIRAL GRAVITY
====================================================================

Assume:
    • low-ℓ temperature anomalies dominated by Q-boundary mode M_{2,3} and dipole gradient
    • chiral sourcing tied to same boundary structure via Π M_{2,3}

Temperature:
    a_{ℓm}^{T,Π} ∼ γ_Π ⟨ Y_{ℓm}, Π M_{2,3} ⟩

Polarization:
    a_{ℓm}^{B,Π} ∼ ε_Π ⟨ {}_2Y_{ℓm}, Π M_{2,3} ⟩
    a_{ℓm}^{E,Π} ∼ E_Π ⟨ {}_2Y_{ℓm}, Π M_{2,3} ⟩   (E_Π some chiral E-coupling)

Then for ℓ ≲ 30:

    • support concentrated near ℓ ≈ 2–3 (Q-boundary mode) and low-ℓ tail
    • approximate scaling:

        C_ℓ^{TB} ∝ γ_Π ε_Π |M_{2,3}|^2  S_ℓ^{TB}
        C_ℓ^{EB} ∝ E_Π ε_Π |M_{2,3}|^2 S_ℓ^{EB}

    where:
        S_ℓ^{TB}, S_ℓ^{EB} encode geometric overlap of Π M_{2,3} with spin-2 harmonics

Key low-ℓ behavior:
    • C_ℓ^{TB}, C_ℓ^{EB} peak at ℓ ≈ 2–3
    • fall off rapidly for ℓ > ℓ_c ≈ 30 (same spectral cutoff as M_{2,3})
    • angular dependence modulated along Axis of Evil / Q-boundary direction

Thus:
    low-ℓ TB/EB ≈ “chiral echo” of the quadrupole–octopole alignment,
    with amplitude ∝ γ_Π ε_Π and shape fixed by M_{2,3}.

COVARIANCE BETWEEN PARITY-ODD SPECTRA AND TEMPERATURE ANOMALIES
===============================================================

Relevant quantities:
    • A_Q  = amplitude of Q-boundary mode in T
    • A_D  = amplitude of hemispherical asymmetry in T
    • γ_Π  = chiral ISW amplitude
    • ε_Π  = chiral polarization coupling

Temperature anomaly sector:
    T(n^) = T_iso(n^) + A_Q M_{2,3}(n^) + A_D (d^ · n^)

Chiral TB/EB sector:
    C_ℓ^{TB} = (1 / (2ℓ + 1)) Σ_m Re[ a_{ℓm}^{T,Π} (a_{ℓm}^{B,Π})* ]
    C_ℓ^{EB} = (1 / (2ℓ + 1)) Σ_m Re[ a_{ℓm}^{E,Π} (a_{ℓm}^{B,Π})* ]

Covariance structure (schematic):

    Cov( A_Q, C_ℓ^{TB} ) ∝
        ⟨ M_{2,3}, Π M_{2,3} ⟩ γ_Π ε_Π

    Cov( A_Q, C_ℓ^{EB} ) ∝
        ⟨ M_{2,3}, Π M_{2,3} ⟩ E_Π ε_Π

    Cov( A_D, C_ℓ^{TB/EB} ) ∝
        directional overlap between d^ and Π M_{2,3}

Interpretation:
    • stronger quadrupole–octopole alignment (larger A_Q) → larger correlated TB/EB
    • stronger hemispherical asymmetry (larger A_D) → directional modulation of TB/EB
    • all four parameters (A_Q, A_D, γ_Π, ε_Π) tied by RCG boundary dynamics

Key prediction:
    • nonzero TB/EB must:
        – align with Axis of Evil / Q-boundary mode
        – correlate with measured A_Q, A_D
    • ΛCDM baseline: C_ℓ^{TB} = C_ℓ^{EB} = 0 (no such covariance)

So:
    detection of TB/EB with preferred-axis alignment and covariance with low-ℓ T anomalies
    is a smoking-gun signature for RCG + chiral gravity.

LOW-ℓ SCALING BEHAVIOR (RCG + CHIRAL GRAVITY)
=============================================

Given:
    a_{ℓm}^{T,Π} ∝ γ_Π ⟨ Y_{ℓm}, Π M_{2,3} ⟩
    a_{ℓm}^{B,Π} ∝ ε_Π ⟨ {}_2Y_{ℓm}, Π M_{2,3} ⟩

Parity-odd spectra:
    C_ℓ^{TB} = (1/(2ℓ+1)) Σ_m Re[ a_{ℓm}^{T,Π} (a_{ℓm}^{B,Π})* ]
    C_ℓ^{EB} = (1/(2ℓ+1)) Σ_m Re[ a_{ℓm}^{E,Π} (a_{ℓm}^{B,Π})* ]

Low-ℓ structure:
    • Π M_{2,3} has spectral support at ℓ = 2,3
    • negligible power for ℓ > ℓ_c ≈ 30

Scaling:
    C_ℓ^{TB} ∼ γ_Π ε_Π |M_{2,3}|^2 S_ℓ^{TB}
    C_ℓ^{EB} ∼ E_Π ε_Π |M_{2,3}|^2 S_ℓ^{EB}

Where:
    S_ℓ^{TB}, S_ℓ^{EB} are geometric overlap factors.

Behavior:
    • peak at ℓ = 2–3
    • rapid suppression for ℓ > 30
    • amplitude ∝ γ_Π ε_Π and tied to A_Q via boundary dynamics

Interpretation:
    TB/EB at low ℓ are a “chiral echo” of the Q-boundary mode.


PLANCK CONSTRAINTS ON PARITY-ODD SIGNALS
========================================

ΛCDM baseline:
    C_ℓ^{TB} = 0
    C_ℓ^{EB} = 0
    (no parity violation or birefringence)

Planck observations:
    • TB/EB consistent with zero within uncertainties
    • small but non-zero cosmic birefringence angle:
          β ≈ 0.3°  (≈ 3–4σ depending on dataset)
    • no evidence for large-scale parity-odd power

Implications for RCG + chiral gravity:
    • predicted TB/EB must satisfy:
          |C_ℓ^{TB}|, |C_ℓ^{EB}| ≪ C_ℓ^{TT}, C_ℓ^{EE}
    • strongest constraints at ℓ = 2–3 (where RCG predicts peaks)
    • requires:
          |γ_Π ε_Π|  small enough to remain within Planck bounds

Directional test:
    • any real TB/EB signal must align with:
          – Axis of Evil
          – Q-boundary mode M_{2,3}
    • amplitude must correlate with A_Q and A_D

Key discriminator:
    ΛCDM → no preferred-axis TB/EB
    RCG+chiral → axis-aligned, low-ℓ localized TB/EB with amplitude tied to anomaly sector

Thus:
    Planck constraints bound γ_Π ε_Π
    Future detection of axis-aligned TB/EB would strongly favor RCG + chiral gravity.

=========================================================
See also: 
CHIRAL ISW CONTRIBUTION 
https://github.com/JDRedding/RCG-Relational-Cosmological-Geometry/blob/main/CHIRAL%20ISW%20CONTRIBUTION

